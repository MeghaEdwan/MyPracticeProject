import React from 'react';

function Props(props) {
    return (
        <div>
            <h3>We learn props in {props.language}</h3>
            <h4>Props are used to communicate between the components</h4>
        
        </div>
    );
}

export default Props;